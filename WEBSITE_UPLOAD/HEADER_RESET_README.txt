Header reset version

This version deliberately rolls back the later logo-image edits.

- The original DS Han Lab logo PNG is used untouched.
- No cropping, reconstruction, or rescaling of text inside the PNG.
- HOME / RESEARCH / PEOPLE / PUBLICATIONS / NEWS / NEWS-POST all load
  one common file: header-brand.css
- Logo display size is based on the People-page version:
  desktop height 82 px; mobile height 66 px.
- Existing site content, including the two added undergraduate interns,
  is preserved.

If the small tagline inside the logo still needs to be larger, the safe next
step is to redesign/export the logo once as a new source image rather than
cropping and recompositing the existing PNG.
